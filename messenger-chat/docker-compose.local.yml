version: '3.3'

services:
  postgres:
    image: postgres:latest
    ports:
      - "10102:5432"
    environment:
      POSTGRES_DB: messenger-chat
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres